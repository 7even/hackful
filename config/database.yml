#this works on Ubuntu Linux, change for your OS.
development:
  adapter: postgresql
  encoding: utf8
  database: hackful
  username: kiote
  password:
  host: localhost

test: &test
  adapter: mysql2
  encoding: utf8
  database: hackful_test
  username: root
  password:
  socket: /var/run/mysqld/mysqld.sock

cucumber:
  <<: *test
